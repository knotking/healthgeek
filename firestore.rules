rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Note: this is a very permissive ruleset.
    // In a production app, you should lock this down.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
